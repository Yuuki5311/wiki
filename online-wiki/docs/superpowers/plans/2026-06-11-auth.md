# Wiki 密码认证实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Wiki Web 加固定密码保护，未登录用户只看到密码输入页，输入正确密码后进入完整界面。

**Architecture:** 后端 `/auth/login` 改为单密码校验，签发 JWT；`/api/wiki` 路由恢复 `requireAuth`。前端新增登录页和 auth store，所有 API 请求携带 token，App.tsx 根据 token 存在与否切换界面。

**Tech Stack:** Express + JWT（后端已有），React + Zustand + localStorage（前端）

---

## 文件总览

**后端（wiki-server）：**
- Modify: `src/routes/auth.ts` — 改为单密码校验
- Modify: `src/index.ts` — 恢复 `/api/wiki` 的 `requireAuth`
- Modify: `.env`（云服务器）— 新增 `WIKI_PASSWORD=123456`

**前端（wiki-web）：**
- Create: `src/api/auth-api.ts` — 登录 API
- Create: `src/stores/auth-store.ts` — token 状态管理
- Create: `src/components/auth/login-page.tsx` — 登录页
- Modify: `src/api/wiki-api.ts` — 所有请求加 Authorization header
- Modify: `src/api/graph-api.ts` — 所有请求加 Authorization header
- Modify: `src/App.tsx` — 无 token 时显示登录页

---

## Task 1: 后端——改单密码登录 + 恢复认证

**Files:**
- Modify: `online-wiki/wiki-server/src/routes/auth.ts`
- Modify: `online-wiki/wiki-server/src/index.ts`

- [ ] **Step 1: 修改 `src/routes/auth.ts`，改为单密码校验**

完整替换文件内容：
```typescript
import { Router } from 'express'
import { signToken } from '../middleware/auth'

export const authRouter = Router()

authRouter.post('/login', (req, res) => {
  const { password } = req.body as { password?: string }

  if (!password) {
    res.status(400).json({ error: '缺少 password' })
    return
  }

  const expected = process.env.WIKI_PASSWORD
  if (!expected || password !== expected) {
    res.status(401).json({ error: '密码错误' })
    return
  }

  const token = signToken({ userId: 'wiki-user', email: 'wiki@internal' })
  res.json({ token, expiresIn: 7 * 24 * 60 * 60 })
})
```

- [ ] **Step 2: 修改 `src/index.ts`，恢复 `/api/wiki` 的 `requireAuth`**

将：
```typescript
import { wikiRouter } from './routes/wiki'
import { ingestRouter } from './routes/ingest'
import { searchRouter } from './routes/search'
import { authRouter } from './routes/auth'
import { graphRouter } from './routes/graph'

import { createChatServer } from './ws/chat-handler'
import { mcpRouter } from './routes/mcp'
import { requireApiKey } from './middleware/api-key'
```
改为：
```typescript
import { wikiRouter } from './routes/wiki'
import { ingestRouter } from './routes/ingest'
import { searchRouter } from './routes/search'
import { authRouter } from './routes/auth'
import { graphRouter } from './routes/graph'
import { requireAuth } from './middleware/auth'
import { createChatServer } from './ws/chat-handler'
import { mcpRouter } from './routes/mcp'
import { requireApiKey } from './middleware/api-key'
```

将：
```typescript
// 公开路由（wiki 读写）
app.use('/api/wiki', wikiRouter)
app.use('/api/wiki', ingestRouter)
app.use('/api/wiki', searchRouter)
app.use('/api/wiki', graphRouter)
```
改为：
```typescript
// JWT 认证路由
app.use('/api/wiki', requireAuth, wikiRouter)
app.use('/api/wiki', requireAuth, ingestRouter)
app.use('/api/wiki', requireAuth, searchRouter)
app.use('/api/wiki', requireAuth, graphRouter)
```

- [ ] **Step 3: 编译验证**

```bash
cd /Users/l/Desktop/wiki/online-wiki/wiki-server
npm run build
```
期望：无报错

- [ ] **Step 4: 在云服务器 .env 新增 WIKI_PASSWORD**

```bash
sshpass -p 'p6LyzXRSnwMvMM8OQo6' ssh -o StrictHostKeyChecking=no root@124.71.66.20 \
  "grep -q WIKI_PASSWORD /root/wiki-server/.env || echo 'WIKI_PASSWORD=123456' >> /root/wiki-server/.env"
```

- [ ] **Step 5: 上传并重启**

```bash
sshpass -p 'p6LyzXRSnwMvMM8OQo6' scp -o StrictHostKeyChecking=no \
  /Users/l/Desktop/wiki/online-wiki/wiki-server/src/routes/auth.ts \
  root@124.71.66.20:/root/wiki-server/src/routes/auth.ts

sshpass -p 'p6LyzXRSnwMvMM8OQo6' scp -o StrictHostKeyChecking=no \
  /Users/l/Desktop/wiki/online-wiki/wiki-server/src/index.ts \
  root@124.71.66.20:/root/wiki-server/src/index.ts

sshpass -p 'p6LyzXRSnwMvMM8OQo6' ssh -o StrictHostKeyChecking=no root@124.71.66.20 \
  'cd /root/wiki-server && docker compose build --no-cache wiki-server wiki-worker 2>&1 | tail -3 && docker compose up -d wiki-server wiki-worker'
```

- [ ] **Step 6: 验证登录接口**

```bash
# 密码正确，应返回 token
curl -s -X POST http://124.71.66.20:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"password":"123456"}' | python3 -c "import sys,json; d=json.load(sys.stdin); print('token OK' if 'token' in d else d)"
```
期望：`token OK`

```bash
# 密码错误，应返回 401
curl -s -o /dev/null -w "%{http_code}" -X POST http://124.71.66.20:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"password":"wrong"}'
```
期望：`401`

```bash
# 无 token 访问 /api/wiki，应返回 401
curl -s -o /dev/null -w "%{http_code}" http://124.71.66.20:3000/api/wiki/company/pages
```
期望：`401`

---

## Task 2: 前端——auth API + store + 登录页

**Files:**
- Create: `online-wiki/wiki-web/src/api/auth-api.ts`
- Create: `online-wiki/wiki-web/src/stores/auth-store.ts`
- Create: `online-wiki/wiki-web/src/components/auth/login-page.tsx`

- [ ] **Step 1: 创建 `src/api/auth-api.ts`**

```typescript
const AUTH_BASE = 'http://124.71.66.20:3000/auth'

export async function loginWithPassword(password: string): Promise<string> {
  const res = await fetch(`${AUTH_BASE}/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ password }),
  })
  if (res.status === 401) throw new Error('密码错误')
  if (!res.ok) throw new Error('登录失败，请稍后重试')
  const data = await res.json() as { token: string }
  return data.token
}
```

- [ ] **Step 2: 创建 `src/stores/auth-store.ts`**

```typescript
import { create } from 'zustand'
import { loginWithPassword } from '@/api/auth-api'

const TOKEN_KEY = 'wiki_token'

interface AuthState {
  token: string | null
  error: string | null
  loading: boolean
  login: (password: string) => Promise<void>
  logout: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  token: localStorage.getItem(TOKEN_KEY),
  error: null,
  loading: false,

  login: async (password: string) => {
    set({ loading: true, error: null })
    try {
      const token = await loginWithPassword(password)
      localStorage.setItem(TOKEN_KEY, token)
      set({ token, loading: false })
    } catch (e) {
      set({ error: String(e).replace('Error: ', ''), loading: false })
    }
  },

  logout: () => {
    localStorage.removeItem(TOKEN_KEY)
    set({ token: null, error: null })
  },
}))

export function getToken(): string | null {
  return localStorage.getItem(TOKEN_KEY)
}
```

- [ ] **Step 3: 创建 `src/components/auth/login-page.tsx`**

```tsx
import { useState } from 'react'
import { useAuthStore } from '@/stores/auth-store'
import { Loader2, Lock } from 'lucide-react'

export function LoginPage() {
  const [password, setPassword] = useState('')
  const { login, loading, error } = useAuthStore()

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    if (!password.trim()) return
    await login(password)
  }

  return (
    <div className="min-h-screen bg-gray-950 flex items-center justify-center">
      <div className="bg-gray-900 border border-gray-800 rounded-lg p-8 w-full max-w-sm">
        <div className="flex flex-col items-center mb-6">
          <div className="w-12 h-12 bg-blue-600/20 rounded-full flex items-center justify-center mb-3">
            <Lock size={22} className="text-blue-400" />
          </div>
          <h1 className="text-lg font-semibold text-white">Wiki 知识库</h1>
          <p className="text-sm text-gray-400 mt-1">请输入访问密码</p>
        </div>
        <form onSubmit={handleSubmit} className="flex flex-col gap-3">
          <input
            type="password"
            value={password}
            onChange={e => setPassword(e.target.value)}
            placeholder="输入密码"
            autoFocus
            className="w-full bg-gray-800 border border-gray-700 rounded px-3 py-2.5 text-sm text-white placeholder-gray-500 focus:outline-none focus:border-blue-500"
          />
          {error && (
            <p className="text-xs text-red-400">{error}</p>
          )}
          <button
            type="submit"
            disabled={loading || !password.trim()}
            className="w-full py-2.5 bg-blue-600 hover:bg-blue-500 disabled:opacity-50 rounded text-sm text-white font-medium flex items-center justify-center gap-2 transition-colors"
          >
            {loading && <Loader2 size={14} className="animate-spin" />}
            进入
          </button>
        </form>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 编译验证**

```bash
cd /Users/l/Desktop/wiki/online-wiki/wiki-web
npm run build
```
期望：无报错

---

## Task 3: 前端——API 加 token + App.tsx 路由守卫

**Files:**
- Modify: `online-wiki/wiki-web/src/api/wiki-api.ts`
- Modify: `online-wiki/wiki-web/src/api/graph-api.ts`
- Modify: `online-wiki/wiki-web/src/App.tsx`

- [ ] **Step 1: 修改 `src/api/wiki-api.ts`，所有请求加 Authorization header**

在文件顶部导入行后新增一个工具函数，并更新所有 fetch 调用：

完整替换文件为：
```typescript
import { API_BASE, WIKI_ID } from '@/config'
import { getToken } from '@/stores/auth-store'

function authHeaders(): HeadersInit {
  const token = getToken()
  return token ? { Authorization: `Bearer ${token}` } : {}
}

export interface PageSummary {
  path: string
  title: string
}

export interface SearchResult {
  path: string
  title: string
  excerpt: string
  score: number
}

export async function listPages(): Promise<PageSummary[]> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/pages`, { headers: authHeaders() })
  if (!res.ok) throw new Error('获取页面列表失败')
  const data = await res.json() as { pages: string[] }
  return data.pages.map(path => ({
    path,
    title: path.replace(/^wiki\//, '').replace(/\.md$/, ''),
  }))
}

export async function readPage(pageId: string): Promise<string> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/pages/${encodeURIComponent(pageId)}`, { headers: authHeaders() })
  if (!res.ok) throw new Error(`读取页面失败: ${pageId}`)
  const data = await res.json() as { content: string }
  return data.content
}

export async function savePage(pageId: string, content: string): Promise<void> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/pages/${encodeURIComponent(pageId)}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...authHeaders() },
    body: JSON.stringify({ content }),
  })
  if (!res.ok) throw new Error('保存页面失败')
}

export async function searchPages(query: string): Promise<SearchResult[]> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/search?q=${encodeURIComponent(query)}`, { headers: authHeaders() })
  if (!res.ok) throw new Error('搜索失败')
  const data = await res.json() as { results: SearchResult[] }
  return data.results
}

export async function submitIngest(fileName: string, content: string): Promise<string> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/ingest`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...authHeaders() },
    body: JSON.stringify({ sourceFileName: fileName, sourceContent: content }),
  })
  if (!res.ok) throw new Error('提交失败')
  const data = await res.json() as { jobId: string }
  return data.jobId
}

export type JobStatus =
  | { status: 'queued' }
  | { status: 'processing'; step: string }
  | { status: 'done'; pagesWritten: string[] }
  | { status: 'error'; error: string }

export async function getJobStatus(jobId: string): Promise<JobStatus> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/jobs/${jobId}`, { headers: authHeaders() })
  if (!res.ok) throw new Error('查询任务状态失败')
  return res.json() as Promise<JobStatus>
}
```

- [ ] **Step 2: 修改 `src/api/graph-api.ts`，所有请求加 Authorization header**

完整替换文件为：
```typescript
import { API_BASE, WIKI_ID } from '@/config'
import { getToken } from '@/stores/auth-store'

function authHeaders(): HeadersInit {
  const token = getToken()
  return token ? { Authorization: `Bearer ${token}` } : {}
}

export interface GraphNode {
  id: string
  title: string
}

export interface GraphEdge {
  source: string
  target: string
  relation: string
  sourceType: 'llm' | 'link'
}

export interface WikiGraph {
  nodes: GraphNode[]
  edges: GraphEdge[]
  updatedAt: string
}

export async function fetchGraph(): Promise<WikiGraph> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/graph`, { headers: authHeaders() })
  if (!res.ok) throw new Error('获取图谱失败')
  return res.json() as Promise<WikiGraph>
}

export async function saveGraph(graph: WikiGraph): Promise<void> {
  const res = await fetch(`${API_BASE}/${WIKI_ID}/graph`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...authHeaders() },
    body: JSON.stringify({ nodes: graph.nodes, edges: graph.edges }),
  })
  if (!res.ok) throw new Error('保存图谱失败')
}
```

- [ ] **Step 3: 修改 `src/App.tsx`，加路由守卫**

完整替换文件为：
```tsx
import { useState } from 'react'
import { NavBar } from '@/components/layout/nav-bar'
import { GraphView } from '@/components/graph/graph-view'
import { PageList } from '@/components/pages/page-list'
import { PageDetail } from '@/components/pages/page-detail'
import { IngestDialog } from '@/components/ingest/ingest-dialog'
import { LoginPage } from '@/components/auth/login-page'
import { usePageStore } from '@/stores/page-store'
import { useAuthStore } from '@/stores/auth-store'

type View = 'graph' | 'pages'

export default function App() {
  const [view, setView] = useState<View>('graph')
  const [ingestOpen, setIngestOpen] = useState(false)
  const { currentPageId, openPage, closePage } = usePageStore()
  const token = useAuthStore(s => s.token)

  if (!token) return <LoginPage />

  const handleNodeClick = (pageId: string) => {
    openPage(pageId)
    setView('pages')
  }

  return (
    <div className="flex flex-col h-screen">
      <NavBar view={view} onViewChange={setView} onIngestOpen={() => setIngestOpen(true)} />
      <main className="flex-1 overflow-hidden">
        {view === 'graph' && <GraphView onNodeClick={handleNodeClick} />}
        {view === 'pages' && (
          currentPageId
            ? <PageDetail onBack={closePage} />
            : <PageList onPageSelect={openPage} />
        )}
      </main>
      {ingestOpen && <IngestDialog onClose={() => setIngestOpen(false)} />}
    </div>
  )
}
```

- [ ] **Step 4: 编译验证**

```bash
cd /Users/l/Desktop/wiki/online-wiki/wiki-web
npm run build
```
期望：无报错，`dist/` 生成

---

## Task 4: 部署前端并验证完整流程

**Files:**
- 上传 `wiki-web/dist/` 到云服务器

- [ ] **Step 1: 上传新 dist**

```bash
cd /Users/l/Desktop/wiki/online-wiki/wiki-web
npm run build

sshpass -p 'p6LyzXRSnwMvMM8OQo6' ssh -o StrictHostKeyChecking=no root@124.71.66.20 \
  'rm -rf /root/wiki-server/wiki-web-dist/* '

sshpass -p 'p6LyzXRSnwMvMM8OQo6' scp -r -o StrictHostKeyChecking=no \
  dist/* root@124.71.66.20:/root/wiki-server/wiki-web-dist/
```

- [ ] **Step 2: 验证页面加载**

```bash
curl -s http://124.71.66.20/ | grep -o '<title>.*</title>'
```
期望：`<title>Wiki 知识库</title>`

- [ ] **Step 3: 验证带 token 可访问 API**

```bash
TOKEN=$(curl -s -X POST http://124.71.66.20:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"password":"123456"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])")

curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer $TOKEN" \
  http://124.71.66.20:3000/api/wiki/company/pages
```
期望：`200`

- [ ] **Step 4: 验证无 token 被拦截**

```bash
curl -s -o /dev/null -w "%{http_code}" http://124.71.66.20:3000/api/wiki/company/pages
```
期望：`401`
